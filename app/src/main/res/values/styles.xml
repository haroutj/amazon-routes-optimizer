<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textViewStyle">@style/MyTextViewStyle</item>
        <item name="editTextStyle">@style/MyEditTextStyle</item>
        <item name="buttonStyle">@style/MyButtonStyle</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">#FAFAFA</item>
    </style>

    <style name="MyButtonStyle" parent="android:Widget.Holo.Button">
        <item name="android:background">@drawable/button_rect</item>
        <item name="android:fontFamily">@font/actor</item>
    </style>

    <style name="MyTextViewStyle" parent="android:Widget.Holo.TextView">
        <item name="android:fontFamily">@font/actor</item>
    </style>

    <style name="MyEditTextStyle" parent="android:Widget.Holo.EditText">
        <item name="android:fontFamily">@font/actor</item>
    </style>

</resources>
